<div class="row">
  <h1 class="page-title"><%= t('user.show.name') %></h1>
  <div class="show-wrapper col-center col-md-6 col-xs-6">
    <h3>
      <span><%= t('user.name') %>:</span> <%= @user.name %>
    </h3>
    <h3>
      <span><%= t('user.email') %>:</span> <%= @user.email %>
    </h3>
    <div class="back-button row">
      <%= link_to t('task.back'), tasks_path, class: "btn btn-default col-md-12"  %>
    </div>
  </div>
</div>